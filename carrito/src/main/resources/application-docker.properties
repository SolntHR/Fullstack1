spring.datasource.url=jdbc:mysql://mysql-carrito:3306/carrito?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD}

eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.instance.hostname=${spring.application.name}

services.inventario.base-url=http://inventario
services.promociones.base-url=http://promociones
